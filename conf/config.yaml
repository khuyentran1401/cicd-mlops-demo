defaults:
  - process: process_2
  - segment: KMeans
  - _self_

data:
  raw: data/raw/marketing_campaign.csv
  intermediate: data/intermediate/scaled_features.pkl
  final: data/final/segmented.pkl

scaler: scaler/standard_scaler.pkl
model: model/kmeans.pkl

elbow_metric: silhouette

image:
  elbow: image/elbow.png
  clusters: image/cluster.png

pca:
  n_components: 3
  columns:
    - col1
    - col2
    - col3
